#include<bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string str;
    getline(cin, str);
    while(getline(cin, str)) {
        string strip = "";
        for(int i = 1; i < str.size(); i++) if(str[i] != ' ') strip += str[i];
        if(str[0] == 'E') {
            string list[]
        } else {

        }
    }
}